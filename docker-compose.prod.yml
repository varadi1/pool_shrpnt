services:
  frontend:
    build:
      context: ./web
      dockerfile: Dockerfile
      target: production
    ports:
      - "3000:3000"
    environment:
      - VITE_API_BASE_URL=${VITE_API_BASE_URL:-http://api:8000}
      - NODE_ENV=production
    volumes: []


